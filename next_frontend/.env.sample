# NextAuth Configuration
NEXTAUTH_SECRET=your-nextauth-secret-here  # Generate with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000

# API Configuration
# API_URL is optional - only needed for Docker development environments
# where server-side and client-side need different URLs
# If not set, server actions will use NEXT_PUBLIC_API_URL
API_URL=http://tea-backend:8000  # Optional: for server-side in Docker
NEXT_PUBLIC_API_URL=http://localhost:8000  # Required: for client-side/browser

# GitHub OAuth Configuration
GITHUB_APP_CLIENT_ID=your-github-client-id
GITHUB_APP_CLIENT_SECRET=your-github-client-secret
