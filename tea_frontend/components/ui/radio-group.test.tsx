// Placeholder to prevent test runner error
import { describe, it } from "vitest";

describe("RadioGroup", () => {
  it("placeholder test", () => {
    // RadioGroup testing is done in share-modal.test.tsx
  });
});
