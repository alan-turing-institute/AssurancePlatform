---
title: "Reflect on your first case"
description: Review the elements and the decisions you made while building a case.
---

import ConceptCarousel from "@/components/docs/curriculum/concept-carousel";
import { brewingConcepts } from "./concepts";
import { brewingReflectionPrompts } from "./reflections";
import ReflectionPrompts from "@/components/docs/curriculum/reflection-prompts";
import { ModuleProgressProvider } from "@/components/docs/curriculum/module-progress-context";
import ModuleProgressTracker from "@/components/docs/curriculum/module-progress-tracker";
import { brewingTasks } from "./tasks";

export const PageContent = ({ children }) => {
  return (
    <ModuleProgressProvider
      courseId="tea-trainee"
      moduleId="brewing-your-own-tea"
      tasks={brewingTasks}
      currentPage="reflection"
    >
      {children}
      <ModuleProgressTracker showHints={true} currentPage="reflection" />
    </ModuleProgressProvider>
  );
};

<PageContent>

## The Core Elements

<div id="core-elements" className="mt-8">

<ConceptCarousel
  concepts={brewingConcepts}
  mode="guided"
  taskId="review-core-elements"
/>

</div>

<div id="reflection-prompts" className="mt-8">

<ReflectionPrompts
  prompts={brewingReflectionPrompts}
  allowSkip={true}
  autoSave={true}
  showProgress={true}
  taskId="complete-reflection"
/>

</div>

Write brief answers in your own project vocabulary. The value is in identifying a gap and the next action, not in making every branch appear complete.

</PageContent>
